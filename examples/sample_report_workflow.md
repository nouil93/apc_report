# Sample workflow

1. Copy `config.example.yaml` to `config.yaml`
2. Update the APC URLs and credentials
3. Run `apc-report report --config ./config.yaml`
4. Review generated PDFs and CSV exports in the output directory
5. Adjust HTML selectors in `scraper.py` if your APC firmware version differs
